# Initial data for objects and obstacles

objects:

  pole:
    position: [0, 4, -2] # x/y/z
    yaw: 0 # Just yaw, we don't care about the other two  

    # x,y,z,yaw covariances (higher = more uncertain)
    # units m^2
    covariance: [.5, .5, .000001, .000001]